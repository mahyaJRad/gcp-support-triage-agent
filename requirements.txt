# --- GCP SDKs ---
google-cloud-bigquery>=3.25
google-cloud-bigquery-storage>=2.27   # fast Arrow reads into pandas
google-cloud-language>=2.14           # Natural Language API
google-cloud-aiplatform>=1.71         # Vertex AI (Gemini)
google-cloud-firestore>=2.19          # optional cross-session memory
db-dtypes>=1.3                        # BigQuery -> pandas dtype support

# --- Agent framework ---
google-adk>=1.0                       # Agent Development Kit (local: `adk web`)

# --- Data / NLP ---
pandas>=2.2
numpy>=2.0
spacy>=3.7                            # optional traditional-NLP baseline
beautifulsoup4>=4.12                  # strip HTML from SO post bodies
lxml>=5.3

# --- Notebooks / EDA ---
matplotlib>=3.9                       # EDA plots (notebooks/01_eda.ipynb, make eda)
jupyterlab>=4.2                       # open / run the EDA notebook locally
ipykernel>=6.29                       # Jupyter kernel for the support-triage env

# --- Dev tooling ---
python-dotenv>=1.0
ruff>=0.6
pytest>=8.3
